package lib_class;

import java.awt.Color;
import java.awt.Font;
import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

public class funcs {
	
	//Math.random();     generates random number between [0,1)
	//if (m.x==(int)m.x){} check if it is int  
	
	//Scanner keyboard = new Scanner(System.in);
	//System.out.println("enter two integer to set size");
	//int sizex = keyboard.nextInt();
	//int sizey = keyboard.nextInt();	
	
	//Integer.parseInt(first);         			//String to Integer
	//Integer.valueOf(string_number);			//String to Integer
	//String num=Integer.toString(integer); 	//Integer to String
	//String num = String.valueOf(i);			//Integer to String	
	//number.charAt(0)                 			//to get char of a String                 number is a String
	//last=num.substring(3,4)					//get substring of a string

	//List<Integer> divisor_numbers = new ArrayList<>();
	
	
	//p.drawString(m.x+" , "+m.y,200,200);
	//p.drawLine(0, 500,800 ,500);          // to make a line 
	//p.drawRect(200, 200, 200, 200);
	//p.fillRect(200, 200, 200, 200);
	//setBackground(Color.yellow);
	//p.setColor(Color.blue); 
	//int height =p.getFontMetrics().getHeight();          p is a graphics class
	//int width  =p.getFontMetrics().stringWidth("aaaaa");
	
	//Font tt=new Font("Times New Roman", Font.PLAIN, 50)
	//p.setFont(tt);

	
	//MOUSE
	//m=e.getPoint();	e is a MouseEvent
	
	//Thread.sleep(100);

	
	//KANVAS
	//addMouseListener(this);
	//addMouseMotionListener(this);
	//setBackground(Color.red);
	
}